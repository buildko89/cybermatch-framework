{
  "schema_version": "1.0",
  "id": "agentic_credential_chain_v1",
  "version": "1.0",
  "title": "Agentic credential amplification chain",
  "hypothesis": "An autonomous workload may convert one secret discovery into credential reuse and privilege escalation.",
  "source": "observed",
  "required_fields": [
    "campaign_id",
    "actor_id",
    "event_id",
    "event_type",
    "step"
  ],
  "group_by": [
    "campaign_id",
    "actor_id"
  ],
  "operations": [
    {
      "operator": "sequence",
      "items": [
        {"event_type": "secret_discovery"},
        {"event_type": ["credential_reuse", "token_mint"], "within_steps": 3},
        {"event_type": ["privilege_escalation", "control_plane_access"], "within_steps": 4}
      ],
      "max_span_steps": 7,
      "overlap": false
    }
  ],
  "finding": {
    "severity": "critical",
    "title": "Autonomous workload amplified credential access",
    "reason": "Observed secret discovery progressed through reuse or token minting to elevated access.",
    "score": 0.99
  },
  "metadata": {
    "tags": ["agentic", "identity", "credential", "sequence"]
  }
}
